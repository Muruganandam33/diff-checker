apiVersion: v1
kind: Service
metadata:
  name: diff-checker
  namespace: diff-checker
spec:
  type: NodePort
  selector:
    app: diff-checker
  ports:
    - port: 80
      targetPort: 5000
      nodePort: 30081
